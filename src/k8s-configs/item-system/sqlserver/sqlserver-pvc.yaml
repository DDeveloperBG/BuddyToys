kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sql-server-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: sql-server-storageclass
  selector:
    matchLabels:
      release: "stable"
